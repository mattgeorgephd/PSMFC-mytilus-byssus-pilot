# 03_analyses

Generated outputs. Each subfolder is named for the script in `../01_code/` that
produces it. Treat everything here as regenerable; the inputs live in `../02_data/`.

| Subfolder | Produced by | Contents |
|-----------|-------------|----------|
| `extract-tensometer-data/QC_plots/{control,treatment}/` | `1_extract_tensometer_data.Rmd` | Per-thread loess QC plots (`<mussel>_<thread>.jpg`) |
| `extract-tensometer-data/summarized_data/` | `1_extract_tensometer_data.Rmd` | `max_force_*.xlsx`, `integral_*.xlsx` (peak force and area under the force/time curve) |
| `analyze-thread-strength/` | `2_analyze_thread_strength.Rmd` | Adhesion boxplots and per-mussel line graphs by treatment (`.png`, `.pptx`) |

Note: some files in `analyze-thread-strength/` (the `.pptx` decks and a few
`*_MeanThreads.png` variants) were exported manually or by code paths that are
currently commented out in script 2; they are kept here for provenance but are
not all regenerated by a fresh run.
